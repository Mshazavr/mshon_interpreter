fn fib(i) {
    if i - 0 {
        if i - 1 {
            return fib(i-1)+fib(i-2);
        }
        else {
            return 1;
        }
    }
    else {
        return 1;
    }
}

print fib(5);

fn sum(a, b) { 
    return a + b;
} 

let x = 6;
let y = x;
y = y + 1;

print -1 * (fib(x) + fib(7));